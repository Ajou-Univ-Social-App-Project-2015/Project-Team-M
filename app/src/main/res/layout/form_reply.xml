<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Tool bar -->
    <include android:id="@+id/tool_bar" layout="@layout/tool_bar"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:id="@+id/replyview"
        android:layout_gravity="center_horizontal"
        android:layout_below="@+id/tool_bar" />

    <EditText
        android:layout_width="247dp"
        android:layout_height="wrap_content"
        android:id="@+id/replycontent"
        android:layout_alignBottom="@+id/replyimagebutton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="36dp"
        android:layout_marginStart="36dp"
        android:hint="Reply.."/>

    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/reply_icon"
        android:id="@+id/replyimagebutton"
        android:layout_marginTop="43dp"
        android:layout_below="@+id/replyview"
        android:layout_toRightOf="@+id/replycontent"
        android:layout_toEndOf="@+id/replycontent" />

</RelativeLayout>